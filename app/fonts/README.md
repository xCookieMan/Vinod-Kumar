This folder is reserved for local font files if you choose to add them manually.
Currently the layout references local font paths that should be replaced with actual font files or converted to next/font/google usage when fonts are available.
